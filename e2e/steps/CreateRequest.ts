import { Given, When, Then } from 'cypress-cucumber-preprocessor/steps';
import { DataTable } from '@cucumber/cucumber';

Given(/^There is a request created with name "([^"]*)", description "([^"]*)", price (\d+) and requester "([^"]*)"$/, function () {

});
